.col-md-10
  %h2
    = @school_group.name
  %h3
    Administrator for current school group
  - if @admin.blank?
    .alert.alert-warning
      Current school group hasn't administrator yet!
  - else
    = render partial: 'partials/users_preview', collection: @admin, as: :user
  %br

  = link_to 'Edit', edit_school_group_path(@school_group), class: 'btn btn-primary'
  = link_to 'Back', school_groups_path
  %h3
    Schools in current school group
  .panel.panel-default.panel-table
    .panel-heading
      .row
        .col.col-xs-6
          .h3{class: 'panel-title'}
            Edit schools below
        .col.col-xs-6.text-right
          = link_to 'New School', new_school_path + "/#{@school_group.id}", class: 'btn btn-sm btn-primary'
    .panel-body
      - if !@school_group.schools.empty?
        %table{class: "table table-striped table-bordered table-list"}
          %thead
            %tr
              %th
                %em{class: "fa fa-cog"}
              %th{class: "hidden-xs col col-lg-1"}
                ID
              %th
                School Name
          %tbody
            - @school_group.schools.each do |school|
              %tr
                %td{align: "center"}
                  %a{class: "btn btn-default", href: "#{edit_school_path(school)}"}
                    %em{class: "fa fa-pencil"}
                  = link_to school, data: {:confirm => 'Are you sure?'}, :method => :delete, class: "btn btn-danger" do
                    %em{class: "fa fa-trash"}
                %td{class: "hidden-xs"}
                  = school.id
                %td
                  = school.name
                  = link_to 'More...', school_path(school), class: "col col-lg-1", style: "float: right"
      - else
        %br
        .col-lg-7
          .alert.alert-warning
            There is no schools in current group yet but you may create one!

